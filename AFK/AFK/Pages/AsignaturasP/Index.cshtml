@page
@model AFK.Pages.AsignaturasP.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Asignatura[0].NombreAsignatura)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Asignatura) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreAsignatura)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IDAsignatura">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IDAsignatura">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IDAsignatura">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
